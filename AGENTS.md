# AGENTS.md - Home Stock 開発ガイド

## プロジェクト概要

- プロジェクト名: Home Stock
- 目的: 家庭の日用品・食料品について、在庫、期限、入出庫履歴、補充候補を管理する。
- 詳細な要件と構成は `README.md` および `docs/` を参照する。
- `WebAPI/` がAPIとPWA、`WinApp/HomeStock.Windows/` がWindowsクライアント、`PrintWorker/` が印刷処理の現行実装である。変更前に `docs/implementation-status.md` と `docs/roadmap.md` を確認する。

## 共通原則

- 既存の設計、命名、ディレクトリ構成を尊重し、依頼範囲外の変更を混在させない。
- 可読性、保守性、正確性を、短さや技巧的な実装より優先する。
- 1つの型、関数、コンポーネントには、明確な1つの責務を持たせる。
- 重複を避ける。ただし、将来の再利用を予測しただけの抽象化は行わない。
- マジックナンバーや意味の不明な文字列は、意味のある名前の定数または設定値にする。
- 入力値、外部データ、時刻、ネットワーク、永続化処理は失敗し得るものとして扱う。
- 警告を安易に抑制しない。原因を解消できない場合のみ、理由と最小範囲を明示して抑制する。
- 既存の公開 API、データ形式、DB スキーマを変更する場合は、互換性と移行方法を考慮する。
- 秘密情報、アクセストークン、パスワード、実データをソース、設定例、ログへ記録しない。
- コード変更に対応するテストを追加または更新し、関連するテストを実行する。
- ファイル末尾には改行を置き、原則 UTF-8 を使用する。

## コメントとドキュメント

- 公開 API、公開型、公開メンバー、および利用方法や制約が名前だけでは伝わらない内部 API には、各言語で標準のドキュメントコメントを記載する。
- コメントには「何をしているか」の逐語的説明ではなく、目的、契約、前提条件、制約、副作用、例外、設計判断の理由を記載する。
- コードを読めば明らかなコメントは書かない。例: `// 在庫を取得する`、`// i を増やす`。
- 実装変更で古くなるコメントは同時に更新または削除する。
- TODO を残す場合は、未完了の内容と必要な対応を具体的に記載する。課題番号がある場合は併記する。
- コメントアウトしたコードは残さず、バージョン管理の履歴に委ねる。

## C# / .NET

### 書式

- `.editorconfig` が存在する場合はそれを最優先し、通常は `dotnet format` が生成する形式に従う。
- インデントはスペース4つとする。タブは使用しない。
- 波括弧は型、メソッド、プロパティ、制御構文の宣言行の次行に置く。
- `if`、`for`、`foreach`、`while` などは、本文が1行でも原則として波括弧を付ける。
- 1行には原則1つの文と1つの宣言を書く。
- `using` はファイル先頭に置き、不要なものを削除する。`System` 名前空間を先に並べる。
- ファイルスコープ名前空間は、対象プロジェクトの既存スタイルと対象フレームワークが許す場合に使用する。同一プロジェクト内で混在させない。
- 単純なメンバーには式形式を使用してよいが、処理やデバッグが読みにくくなる場合はブロック形式にする。
- 長い引数、コレクション初期化子、クエリは、要素単位で改行し、末尾のカンマを使用できる箇所では付ける。

### 命名

- 名前空間、型、列挙型、メソッド、プロパティ、イベント、公開フィールド、定数は `PascalCase` とする。
- ローカル変数、引数は `camelCase` とする。
- private インスタンスフィールドは `_camelCase` とする。
- インターフェイスには `I` 接頭辞を付ける。型パラメーターには `T` または `TEntity` のような `T` 接頭辞を付ける。
- 非同期メソッドには `Async` 接尾辞を付ける。ただしイベントハンドラーなど、フレームワーク規約と競合する場合を除く。
- `bool` の名前は `is`、`has`、`can`、`should` など、真偽の意味が読める名前にする。
- 略語も通常の単語として扱う。例: `HttpClient`、`JwtToken`、`ProductId`。
- 意味のない短縮名や型を表すハンガリアン記法を使用しない。短いループ添字など、ごく狭い範囲では `i` 等を使用してよい。
- コレクションには複数形、単一要素には単数形を使用する。

### 言語機能と設計

- 型が右辺から明白、または型名を繰り返すことで可読性が下がる場合は `var` を使用する。型が推測しにくい場合は明示する。
- 組み込み型には `string`、`int`、`bool` などの C# キーワードを使用する。
- 文字列の連結には、可読性に応じて文字列補間を使用する。
- null 非許容参照型を有効にし、`!` による抑制よりデータフローと契約を修正する。
- null 判定には `is null` / `is not null` を使用する。
- 変更不要なフィールドは `readonly`、継承不要な型は必要に応じて `sealed` とする。
- 非同期 I/O は `async` / `await` を使用し、同期的にブロックする `.Result`、`.Wait()`、`GetAwaiter().GetResult()` を避ける。
- ライブラリ層では、呼び出し元から受け取った `CancellationToken` を下位の非同期処理へ伝播する。
- 例外は異常系に使用し、通常の分岐に使用しない。捕捉する場合は、処理、文脈追加、または適切な変換ができる場所に限定する。
- 例外を再送出する場合はスタックトレースを保つため `throw;` を使用する。
- `IDisposable` / `IAsyncDisposable` は `using` / `await using` で確実に破棄する。
- LINQ は意図が明確になる範囲で使用し、複数列挙、隠れた DB クエリ、過度に長いチェーンに注意する。
- 金額や正確な十進値には `decimal`、UTC 時刻またはオフセットを伴う時刻には原則 `DateTimeOffset` を使用する。
- ドメインルールは Controller や UI へ埋め込まず、Application または Domain 層へ置く。
- Entity Framework Core のクエリでは N+1、不要な追跡、クライアント評価を避ける。読み取り専用では必要に応じて `AsNoTracking()` を使う。

### XML ドキュメントコメント

- C# のドキュメントコメントは `///` と XML タグを使用する。
- 公開型と公開メンバーには原則 `<summary>` を記載する。
- 引数の意味が自明でない場合は `<param>`、戻り値の意味や特殊値がある場合は `<returns>`、呼び出し側が考慮すべき例外には `<exception>` を記載する。
- `/// <summary>商品を取得します。</summary>` のように名前を言い換えただけの説明は避け、対象範囲、条件、結果の意味を記載する。
- コンパイラ生成ファイル、単純な private DTO、明白なオーバーライドに形式的なコメントを量産しない。

### テスト

- テストは独立して決定的に実行できるようにし、実時刻、実ネットワーク、実行順序へ依存させない。
- テスト名は対象、条件、期待結果が分かる名前にする。既存プロジェクトの命名規則がある場合は合わせる。
- Arrange / Act / Assert の区切りは空行で表現する。補足が必要な場合を除き、それを示すだけのコメントは書かない。
- 正常系だけでなく、境界値、不正入力、権限、競合、在庫不足など重要な異常系を検証する。

## XAML / WPF

- インデントはスペース4つとし、属性が多い要素は1属性1行に分ける。
- コントロール名、リソースキー、バインディング対象は `PascalCase` とする。
- 表示文言、色、寸法、スタイルの重複は、適切な ResourceDictionary またはリソースへまとめる。
- UI の状態と操作は可能な限り ViewModel に置き、コードビハインドはビュー固有の処理に限定する。
- Binding には意図に応じて `Mode` と `UpdateSourceTrigger` を明示し、失敗が見過ごされないようにする。
- 固定座標に依存せず、Grid、StackPanel 等でサイズ変更や表示倍率に耐えるレイアウトにする。
- アクセスキー、ラベル、キーボード操作、フォーカス順、十分なコントラストを考慮する。
- XAML コメントは、複雑なレイアウト制約や回避策の理由が必要な場合だけ記載する。

## JavaScript

- インデントはスペース2つとし、文末にはセミコロンを付ける。
- 文字列は原則シングルクォートを使用する。テンプレート展開や複数行文字列にはテンプレートリテラルを使用する。
- 再代入しない変数は `const`、再代入が必要な変数だけ `let` とし、`var` は使用しない。
- 変数と関数は `camelCase`、クラスは `PascalCase`、定数は既存規約に合わせて `UPPER_SNAKE_CASE` を使用する。
- 厳密等価演算子 `===` / `!==` を使用する。
- コールバックの入れ子より `async` / `await` を優先し、通信失敗と非正常レスポンスを明示的に処理する。
- DOM 要素の取得結果、外部データ、`localStorage` の値を無条件に信用しない。
- DOM 更新時に信頼できない文字列を `innerHTML` へ渡さない。原則 `textContent` を使用する。
- グローバル変数を増やさず、機能単位の関数またはモジュールに分割する。
- 関数は早期 return を用いてネストを浅くし、1つの責務に保つ。
- 公開関数や複雑な契約には JSDoc の `/** ... */` を使用し、必要に応じて `@param`、`@returns`、`@throws` を記載する。
- 処理をそのまま説明する `//` コメントは書かず、ブラウザー制約や設計判断など理由だけを記載する。

## HTML

- HTML5 の意味的な要素を使用し、見た目だけを目的とした要素選択を避ける。
- インデントはスペース2つとし、要素名と属性名は小文字にする。
- `button` には用途に合う `type` を指定する。
- フォーム要素には関連付けられた `label`、適切な `name`、`autocomplete`、入力型を設定する。
- 画像には用途に合う `alt` を設定し、装飾画像は空の `alt` とする。
- 見出し階層を飛ばさず、ランドマーク、キーボード操作、フォーカス表示を維持する。
- JavaScript のフックには `data-*` または明確な ID を使い、スタイル用クラスとの責務を混同しない。
- インライン CSS とインラインイベントハンドラーは使用せず、外部ファイルへ分離する。

## CSS

- インデントはスペース2つとし、1宣言1行、宣言末尾にセミコロンを付ける。
- クラス名は役割が分かる `kebab-case` とし、過度に DOM 構造へ依存したセレクターを避ける。
- ID セレクター、`!important`、高すぎる詳細度を避け、カスケードを予測可能に保つ。
- 色、間隔、フォント等の繰り返す値には CSS カスタムプロパティを使用する。
- モバイルを基準にし、狭い画面、文字拡大、タッチ操作でも利用できるようにする。
- `:focus-visible` を含むフォーカス表示を削除しない。動きには `prefers-reduced-motion` を考慮する。
- コメントはセクションの意図、ブラウザー回避策、非自明な制約に限定する。

## JSON / JSONC

- インデントはスペース2つとする。
- JSON ではコメントと末尾カンマを使用しない。JSONC が明示的に対応するファイルではその仕様に従う。
- キーの順序は既存ファイルとフレームワークの慣例を維持し、無関係な並べ替えをしない。
- 環境固有値や秘密情報をコミットせず、環境変数または安全な秘密情報ストアから与える。

## YAML / Docker Compose

- インデントはスペース2つとし、タブを使用しない。
- 真偽値は `true` / `false`、曖昧に解釈され得る文字列は引用符で囲む。
- Compose のサービス名、ボリューム名、ネットワーク名は、小文字の `kebab-case` または既存規約に合わせる。
- イメージタグ、ポート、永続ボリューム、ヘルスチェック、依存関係を明示し、運用上重要な値を暗黙の既定値に頼らせない。
- シークレットを Compose ファイルへ直接書かず、環境変数または secrets を使用する。
- 開発用と本番用の差分を明確にし、本番設定でデバッグ機能や不要な公開ポートを有効にしない。

## Dockerfile

- 再現性のためベースイメージのバージョンを明示する。
- マルチステージビルドを使用し、実行イメージに SDK、ソース、キャッシュ、不要なツールを含めない。
- キャッシュが効くよう、依存関係の復元に必要なファイルを先にコピーし、その後ソースをコピーする。
- 関連する `RUN` を適切にまとめ、同じレイヤー内で一時ファイルを削除する。
- 可能な限り非 root ユーザーで実行し、必要最小限のファイルと権限だけを与える。
- `ENTRYPOINT` / `CMD` は exec 形式を使用する。
- 秘密情報を `ARG`、`ENV`、イメージレイヤーへ埋め込まない。

## Caddyfile

- ディレクティブは役割ごとにまとめ、インデントを統一する。
- 公開ホスト、TLS、リバースプロキシ先、静的ファイルの責務を明確にする。
- 認証情報をファイルへ直接記載しない。
- セキュリティヘッダー、圧縮、ログは要件に応じて明示し、変更後は Caddy の構文検証を行う。

## Markdown

- ATX 形式の見出し (`#`) を使用し、階層を飛ばさない。
- 見出し、段落、リスト、コードブロックの間には空行を置く。
- 箇条書きは `-`、番号付き手順は `1.` を使用する。
- コードブロックには可能な限り言語名を指定する。
- 相対リンクを使用し、リンク先やコマンドが実際に存在し動作することを確認する。
- 1行の長さを機械的に揃えるためだけの改行は行わず、意味のまとまりを優先する。

## 自動生成ファイルとマイグレーション

- `*.Designer.cs`、`*.g.cs`、生成されたリソースコードなどは直接編集せず、元となる定義または生成手順を変更する。
- EF Core マイグレーションとモデルスナップショットは原則としてツールで生成する。生成済みで共有されたマイグレーションを、影響確認なしに削除・再作成しない。
- ロックファイルや生成物は、プロジェクトで追跡されている場合のみ更新し、変更理由を依存関係の変更と一致させる。

## 変更時の確認

- 変更前に関連ドキュメント、呼び出し元、テスト、設定を確認する。
- C# 変更後は対象ソリューションまたはプロジェクトで `dotnet build` と `dotnet test` を実行する。
- Web UI 変更後は、主要な画面幅、キーボード操作、通信成功時と失敗時を確認する。
- Docker、Compose、Caddy の変更後は、利用可能な構文検証または設定検証を行う。
- 実行できなかった確認がある場合は、未実施の項目と理由を明記する。
